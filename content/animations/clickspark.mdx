---
title: "Click Spark Animation - Pumki UI"
description: A component that renders dynamic spark bursts on click interactions.
---

import { Tabs } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Callout } from 'nextra/components'
import { Preview } from '@/components/docs/preview'
import ClickSpark from '@/components/landing/ui/ClickSpark'
import { ClickSparkDemo } from '@/components/demos/click-spark-demo'

# Click Spark Animation

A component that renders animated spark bursts when the user clicks anywhere inside its container.

<Preview
  code={`import ClickSpark from '@/components/pumki-ui/animations/click-spark';

export const ClickSparkDemo = () => {
  return (
    <div className="relative w-full h-[400px] bg-black flex items-center justify-center text-white">
        <ClickSpark sparkColor="#ff3366" sparkSize={12} sparkRadius={20} sparkCount={10} duration={500}>
            <div className='flex items-center justify-center h-full'>
                <button className="px-6 py-3 bg-pink-600 rounded-lg hover:bg-pink-500 transition">
                    Click Me
                </button>
            </div>
        </ClickSpark>
    </div>
  );
};`}
>
  <ClickSparkDemo />
</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`pnpm dlx pumki add click-spark`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`npx pumki add click-spark`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`yarn dlx pumki add click-spark`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`bun x pumki add click-spark`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br />

<CodeBlock language="typescript">
{`import { ClickSpark } from '@/components/pumki-ui/animations/click-spark';`}
</CodeBlock>

Wrap your clickable area (e.g., button, div) inside `<ClickSpark>` to add burst animations:

<br />

<CodeBlock language="typescript">
{`<ClickSpark sparkColor="#ff3366" sparkSize={12} sparkRadius={20} sparkCount={10} duration={500}>
    <button>Click Me</button>
</ClickSpark>`}
</CodeBlock>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `sparkColor` | `string` | `"#fff"` | Color of the sparks (HEX or CSS color). |
| `sparkSize` | `number` | `10` | Length of each spark line. |
| `sparkRadius` | `number` | `15` | Radius of spark spread from the click point. |
| `sparkCount` | `number` | `8` | Total number of sparks emitted per click. |
| `duration` | `number` | `400` | Duration of spark animation in milliseconds. |
| `easing` | `"linear" \| "ease-in" \| "ease-out" \| "ease-in-out"` | `"ease-out"` | Easing function applied to spark motion. |
| `extraScale` | `number` | `1.0` | Additional scale multiplier for spark distance. |
| `children` | `React.ReactNode` | `-` | Any clickable content to wrap inside the spark animation. |
