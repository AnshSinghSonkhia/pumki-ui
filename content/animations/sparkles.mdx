---
title: "Sparkles - Pumki UI"
---

import { Tabs } from 'nextra/components'
import { Callout } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Preview, ResponsivePreview } from '@/components/docs/preview'
import { Button } from '@/components/ui/button'
import { SparklesCore } from '@/components/pumki-ui/ui/sparkles.tsx'

# Sparkles

<Preview 
  code={`'use client';

import { SparklesCore } from '@/components/pumki-ui/animations/sparkles.tsx'

export const SparklesDemo = () => {
    return (
        <div style={{ width: '100%', height: '600px', position: 'relative' }}>
            <SparklesCore
                id="tsparticles"
                background="transparent"
                particleDensity={500}
                className="absolute inset-x-0 bottom-0 h-full w-full [mask-image:radial-gradient(50%_50%,white,transparent_85%)]"
            />
        </div>
    );
};`}
>
    <SparklesCore
        id="tsparticles"
        background="transparent"
        particleDensity={500}
        className="absolute inset-x-0 bottom-0 h-full w-full [mask-image:radial-gradient(50%_50%,white,transparent_85%)]"
    />
</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`pnpm dlx pumki add sparkles`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`npx pumki add sparkles`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`yarn dlx pumki add sparkles`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`bun x pumki add sparkles`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript" showLineNumbers={false}>
    {`import { SparklesCore } from '@/components/pumki-ui/animations/sparkles.tsx';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript" showLineNumbers={false}>
    {`<SparklesCore />`}
</CodeBlock>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `id` | `string` | `auto-generated` | Unique ID for the particles container. |
| `className` | `string` | `-` | Custom CSS classes for the wrapper div. |
| `background` | `string` | `#0d47a1` | Background color of the animation. |
| `particleSize` | `number` | `-` | Size of the particles (overrides min/max). |
| `minSize` | `number` | `1` | Minimum particle size. |
| `maxSize` | `number` | `3` | Maximum particle size. |
| `speed` | `number` | `4` | Speed of the opacity animation. |
| `particleColor` | `string` | `#ffffff` | Color of the particles. |
| `particleDensity` | `number` | `120` | Number of particles rendered. |
