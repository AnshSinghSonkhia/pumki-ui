---
title: "Space Particles Animation - Pumki UI"
description: A component that renders a Dynamic 3D field of particles in space.
---

import { Tabs } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Callout } from 'nextra/components'
import { Preview, ResponsivePreview } from '@/components/docs/preview'
import { Button } from '@/components/ui/button'
import { Particles } from '@/components/pumki-ui/animations/space-particles'

# Space Particles Animation

A component that renders a Dynamic 3D field of particles in space.

<Preview
  code={`import { Particles } from '@/components/pumki-ui/animations/space-particles';

  export const SpaceParticlesDemo = () => {
    return (
        <div className="relative w-full h-[800px]">
            <Particles
                color="#fffff"
                particleCount={25000}
                particleSize={5}
                animate={false}
                className="z-0 bg-black"
            />
        </div>
    );
};`}
>
        <div className="relative w-full h-[800px]">
            <Particles
                color="#fffff"
                particleCount={25000}
                particleSize={5}
                animate={false}
                className="z-0 bg-black"
            />
        </div>

</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`pnpm dlx pumki add space-particles`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`npx pumki add space-particles`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`yarn dlx pumki add space-particles`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`bun x pumki add space-particles`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript">
    {`import { Particles } from '@/components/pumki-ui/animations/space-particles';`}
</CodeBlock>

<br/>

<Callout type="important">🎨 Particle Asset {`(Required!)`}</Callout>

Download the particle texture for the UI project:  

[![Download Particle](https://img.shields.io/badge/Download-Particle-blue?style=plastic&logo=github)](https://github.com/AnshSinghSonkhia/pumki-ui/blob/main/public/assets/particle.png)  

<Callout type="info">Save it in:</Callout>  

`public/assets/particle.png`

<br/>

<CodeBlock language="typescript">
    {`<Particles
        color="#fffff"
        particleCount={25000}
        particleSize={5}
        animate={false}
        className="z-0"
    />`}
</CodeBlock>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `color` | `string` | `"#ff3366"` | Base color of the particles (HEX). |
| `particleCount` | `number` | `10000` | Total number of particles rendered. |
| `particleSize` | `number` | `35` | Size of each individual particle. |
| `animate` | `boolean` | `true` | Whether particles animate color transitions. |
| `className` | `string` | `""` | Optional class for the outer container. |