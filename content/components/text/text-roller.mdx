---
title: "Text Roller - Pumki UI"
description: A component that applies a rolling animation to text, perfect for emphasizing important information.
---

import { Tabs } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Preview, ResponsivePreview } from '@/components/docs/preview'
import { Button } from '@/components/ui/button'
import { RollingText } from '@/components/pumki-ui/text/text-roller'

# Rolling Text Animation

A component that applies a rolling animation to text, perfect for emphasizing important information.

<Preview
  code={`import { RollingText } from '@/components/pumki-ui/text/text-roller';

  export const RollingTextDemo = () => {
    return (
        <RollingText className="text-4xl" text="Ansh Singh Sonkhia" />;
    );
};`}
>
        <RollingText className="text-4xl" text="Ansh Singh Sonkhia" />

</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`pnpm dlx pumki add text-roller`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`npx pumki add text-roller`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`yarn dlx pumki add text-roller`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`bun x pumki add text-roller`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript">
    {`import { RollingText } from '@/components/pumki-ui/text/text-roller';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript">
    {`<RollingText text="Pumki UI" />`}
</CodeBlock>

## Props

| Prop          | Type            | Default                                          | Description                    |
| ------------- | --------------- | ------------------------------------------------ | ------------------------------ |
| text          | string          | -                                                | Text to animate                |
| children      | React.ReactNode | -                                                | Custom content to roll         |
| transition?   | Transition      | `{ duration: 0.5, delay: 0.1, ease: 'easeOut' }` | Animation transition config    |
| inViewOnce?   | boolean         | true                                             | Animate only once when in view |
| inViewMargin? | string          | 0px                                              | Margin for in-view detection   |
| inView?       | boolean         | false                                            | Force animation in view        |
| className?    | string          | -                                                | Custom class name              |
