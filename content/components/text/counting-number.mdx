---
title: "Counting Number Text - Pumki UI"
description: A number display component that animates values with a smooth counting effect, perfect for stats, metrics, and dashboards.
---

import { Tabs } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Preview, ResponsivePreview } from '@/components/docs/preview'
import { Button } from '@/components/ui/button'
import { CountingNumber } from '@/components/pumki-ui/text/counting-number'

# Counting Number Animation

A number display component that animates values with a smooth counting effect, perfect for stats, metrics, and dashboards.

<Preview
  code={`import { CountingNumber } from '@/components/pumki-ui/text/counting-number';

  export const CountingNumberDemo = () => {
    return (
        <CountingNumber number={new Date().getFullYear()} className="text-4xl" />
    );
};`}
>
        <CountingNumber number={new Date().getFullYear()} className="text-4xl" />

</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`pnpm dlx pumki add counting-number`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`npx pumki add counting-number`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`yarn dlx pumki add counting-number`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`bun x pumki add counting-number`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript">
    {`import { CountingNumber } from '@/components/pumki-ui/text/counting-number';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript">
    {`<CountingNumber number={5555} />`}
</CodeBlock>

## Example

<Preview
  title="Using Decimals"
  code={`import { CountingNumber } from '@/components/pumki-ui/text/counting-number';

  export const CountingNumberDemo = () => {
    return (
        <CountingNumber
            number={12345.67}
            decimalPlaces={2}
            decimalSeparator=","
            className="text-4xl"
            inView
        />
    );
};`}
>
        <CountingNumber
            number={12345.67}
            decimalPlaces={2}
            decimalSeparator=","
            className="text-4xl"
            inView
        />

</Preview>

<Preview
  title="Starting from a specific number"
  description="You can start counting from a specific number by using the `fromNumber` prop."
  code={`import { CountingNumber } from '@/components/pumki-ui/text/counting-number';

  export const CountingNumberDemo = () => {
    return (
        <CountingNumber
            number={new Date().getFullYear()}
            fromNumber={0}
            className="text-4xl"
            inView
        />
    );
};`}
>
        <CountingNumber
            number={new Date().getFullYear()}
            fromNumber={0}
            className="text-4xl"
            inView
        />

</Preview>

## Props

| Prop              | Type    | Default                        | Description                     |
| ----------------- | ------- | ------------------------------ | ------------------------------- |
| decimalPlaces?    | number  | 0                              | Number of decimal places        |
| transition?       | object  | `{ stiffness: 90, damping: 50 }` | Animation transition config     |
| decimalSeparator? | string  | "."                            | Decimal separator character     |
| inViewOnce?       | boolean | true                           | Animate only once when in view  |
| inViewMargin?     | string  | 0px                            | Margin for in-view detection    |
| inView?           | boolean | false                          | Force animation in view         |
| padStart?         | boolean | false                          | Pad with zeros at start         |
| fromNumber?       | number  | 0                              | Start counting from this number |
| number            | number  | -                              | Target number to animate        |
