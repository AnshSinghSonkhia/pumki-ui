---
title: "Draw Line Text - Pumki UI"
description: A smooth text effect that draws characters as strokes and fills them in, perfect for headers and highlights.
---

import { Tabs } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Callout } from 'nextra/components'
import { Preview, ResponsivePreview } from '@/components/docs/preview'
import { Button } from '@/components/ui/button'
import { DrawLineText } from '@/components/pumki-ui/text/draw-line-text'

# Draw Line Text Animation

A smooth text effect that draws characters as strokes and fills them in, perfect for headers and highlights.

<Callout>Perfect for headers and highlights.</Callout>

<Preview
  code={`import { DrawLineText } from '@/components/pumki-ui/text/draw-line-text';

  export const DrawLineTextDemo = () => {
    return (
        <DrawLineText
            className="font-medium"
            fontSize={60}
            strokeWidth={1.5}
            text="Ansh Singh Sonkhia"
            color="var(--color-foreground)"
        />
    );
};`}
>
        <DrawLineText
            className="font-medium"
            fontSize={60}
            strokeWidth={1.5}
            text="Ansh Singh Snkhia"
            color="var(--color-foreground)"
        />

</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`pnpm dlx pumki add draw-line-text`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`npx pumki add draw-line-text`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`yarn dlx pumki add draw-line-text`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`bun x pumki add draw-line-text`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript">
    {`import { DrawLineText } from '@/components/pumki-ui/text/draw-line-text';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript">
    {`<DrawLineText
        className="font-medium"
        fontSize={60}
        strokeWidth={1.5}
        text="Ansh Singh Snkhia"
        color="var(--color-foreground)"
    />`}
</CodeBlock>

## Examples

<Preview
  title="At once"
  description="Set `oneByOne` to `false` to animate all characters together without any stagger."
  code={`import { DrawLineText } from '@/components/pumki-ui/text/draw-line-text';

  export const DrawLineTextDemo = () => {
    return (
        <DrawLineText
            className="font-medium"
            oneByOne={false}
            fontSize={60}
            strokeWidth={1.5}
            text="Ansh Singh Sonkhia"
            color="var(--color-foreground)"
        />
    );
};`}
>
        <DrawLineText
            className="font-medium"
            oneByOne={false}
            fontSize={60}
            strokeWidth={1.5}
            text="Ansh Singh Sonkhia"
            color="var(--color-foreground)"
        />

</Preview>

## Props

| Prop          | Type    | Default | Description                |
| ------------- | ------- | ------- | -------------------------- |
| text          | string  | -       | Text to display            |
| afterFill     | boolean | true    | Show fill after stroke     |
| oneByOne      | boolean | true    | Animate letters one by one |
| fontSize      | number  | 40      | Font size in px            |
| wordSpacing   | number  | 10      | Space between words        |
| strokeWidth   | number  | 1       | Stroke width in px         |
| letterSpacing | number  | 0       | Space between letters      |
| color         | string  | -       | Text color                 |
