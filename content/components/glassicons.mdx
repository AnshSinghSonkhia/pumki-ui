---
title: "GlassIcons - Pumki UI"
---

import { Tabs } from 'nextra/components'
import { Callout } from 'nextra/components'
import { CodeBlock } from '../../components/docs/code-block'
import { Preview, ResponsivePreview } from '../../components/docs/preview'
import { Button } from '../../components/ui/button'
import GlassIcons from '@/components/pumki-ui/GlassIcons.tsx'
import { GlassIconDemo } from '@/components/demos/glassicon-demo.tsx'

# Glass Icons

A collection of glassmorphic icons with various styles and sizes.

<Preview 
  previewHeight='200px'
  code={`'use client';

import GlassIcons from '@/components/pumki-ui/GlassIcons.tsx'
import { FileText, Book, Heart, Cloud, Edit, BarChart2 } from 'lucide-react';

export const GlassIconsDemo = () => {

    const items = [
        { icon: <FileText />, color: 'blue', label: 'Files' },
        { icon: <Book />, color: 'purple', label: 'Books' },
        { icon: <Heart />, color: 'red', label: 'Health' },
        { icon: <Cloud />, color: 'indigo', label: 'Weather' },
        { icon: <Edit />, color: 'orange', label: 'Notes' },
        { icon: <BarChart2 />, color: 'green', label: 'Stats' },
    ];

    return (
        <div style={{ height: '600px', position: 'relative' }}>
            <GlassIcons items={items} className="custom-class"/>
        </div>
    );
};`}
>
    <GlassIconDemo />
</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`pnpm dlx pumki add glassicons`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`npx pumki add glassicons`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`yarn dlx pumki add glassicons`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`bun x pumki add glassicons`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript" showLineNumbers={false}>
    {`import GlassIcons from '@/components/pumki-ui/GlassIcons.tsx';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript">
    {`<GlassIcons />`}
</CodeBlock>

## Props

import { PumkiTable, PumkiTableStyles } from '../../components/docs/PumkiTable'

<PumkiTableStyles />
<PumkiTable>
<table className="pumki-table">
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr><td><code>items</code></td><td><code>GlassIconsItem[]</code></td><td><code>required</code></td><td>Array of icon objects to display. Each item includes <code>icon</code> (React element), <code>color</code> (string or gradient key), <code>label</code> (string), and optional <code>customClass</code> (string).</td></tr>
    <tr><td><code>className</code></td><td><code>string</code></td><td><code>""</code></td><td>Custom CSS classes for the grid container.</td></tr>
  </tbody>
</table>
</PumkiTable>
