---
title: "Animated Search - Pumki UI"
---

import { Tabs } from 'nextra/components'
import { Callout } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Preview } from '@/components/docs/preview'
import AnimatedSearch from '@/components/pumki-ui/form/animated-search.tsx'

# Animated Search

<Preview 
  code={`'use client';

import AnimatedSearch from '@/components/pumki-ui/form/animated-search.tsx';

export const AnimatedSearchDemo = () => {
    return (
            <div style={{ width: '100%', height: '600px', position: 'relative', background: 'linear-gradient(90deg, transparent, deeppink, purple)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <AnimatedSearch />
            </div>
    );
};`}
>
      <div style={{ width: '100%', height: '600px', position: 'relative', background: 'linear-gradient(90deg, transparent, deeppink, purple)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <AnimatedSearch />
      </div>
</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`pnpm dlx pumki add animated-search`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`npx pumki add animated-search`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`yarn dlx pumki add animated-search`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`bun x pumki add animated-search`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript" showLineNumbers={false}>
    {`import AnimatedSearch from '@/components/pumki-ui/form/animated-search.tsx';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript" showLineNumbers={false}>
    {`<AnimatedSearch />`}
</CodeBlock>

## Props

| Prop        | Type   | Default     | Description                                           |
| ----------- | ------ | ----------- | ----------------------------------------------------- |
| className   | string | —           | Optional className for custom styling of the wrapper. |
| placeholder | string | "Search..." | Placeholder text for the input field.                 |
