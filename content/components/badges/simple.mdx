---
title: "Simple Badges - Pumki UI"
description: A badge component with various styles and sizes.
---

import { Tabs } from 'nextra/components'
import { CodeBlock } from '@/components/docs/code-block'
import { Preview, ResponsivePreview } from '@/components/docs/preview'
import { SimpleBadge } from '@/components/pumki-ui/badges/simple'

# Simple Badges

A few examples of simple badges with different styles and sizes.

<Preview 
  code={`import { SimpleBadge } from '@/components/pumki-ui/badges/simple';

export const BadgeDemo = () => {
    return (
        <div className="flex flex-col gap-4">
            {/* Basic variants */}
            <SimpleBadge>Default</SimpleBadge>
            <SimpleBadge variant="secondary">Secondary</SimpleBadge>
            <SimpleBadge variant="destructive">Error</SimpleBadge>
            <SimpleBadge variant="outline">Outline</SimpleBadge>
            
            {/* Shiny variants */}
            <SimpleBadge shiny>Shiny Default</SimpleBadge>
            <SimpleBadge variant="secondary" shiny>Shiny Secondary</SimpleBadge>
            <SimpleBadge variant="destructive" shiny>Shiny Error</SimpleBadge>
            <SimpleBadge variant="outline" shiny>Shiny Outline</SimpleBadge>
            
            {/* Custom shine speed */}
            <SimpleBadge shiny shinySpeed={2}>Fast Shine</SimpleBadge>
            <SimpleBadge shiny shinySpeed={8}>Slow Shine</SimpleBadge>
            
            {/* Custom className */}
            <SimpleBadge className="text-lg px-4 py-1">Large SimpleBadge</SimpleBadge>
        </div>
    );
};`}
>
        <div className="flex flex-col gap-4">
            {/* Basic variants */}
      <SimpleBadge>Default</SimpleBadge>
      <SimpleBadge variant="secondary">Secondary</SimpleBadge>
      <SimpleBadge variant="destructive">Error</SimpleBadge>
      <SimpleBadge variant="outline">Outline</SimpleBadge>
      
      {/* Shiny variants */}
      <SimpleBadge shiny>Shiny Default</SimpleBadge>
      <SimpleBadge variant="secondary" shiny>Shiny Secondary</SimpleBadge>
      <SimpleBadge variant="destructive" shiny>Shiny Error</SimpleBadge>
      <SimpleBadge variant="outline" shiny>Shiny Outline</SimpleBadge>
      
      {/* Custom shine speed */}
      <SimpleBadge shiny shinySpeed={2}>Fast Shine</SimpleBadge>
      <SimpleBadge shiny shinySpeed={8}>Slow Shine</SimpleBadge>
      
      {/* Custom className */}
      <SimpleBadge className="text-lg px-4 py-1">Large SimpleBadge</SimpleBadge>
        </div>

</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`pnpm dlx pumki add badge-simple`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`npx pumki add badge-simple`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`yarn dlx pumki add badge-simple`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash" showLineNumbers={false}>
      {`bun x pumki add badge-simple`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript" showLineNumbers={false}>
    {`import { SimpleBadge } from '@/components/pumki-ui/badges/simple';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript" showLineNumbers={false}>
    {`<SimpleBadge>Pumki UI</SimpleBadge>`}
</CodeBlock>

## Props

| Prop        | Type                                                | Default   | Description                                   |
| ----------- | --------------------------------------------------- | --------- | --------------------------------------------- |
| variant?    | 'default' | 'secondary' | 'destructive' | 'outline' | "default" | Style variant for the badge.                  |
| shiny?      | boolean                                             | false     | If true, adds a shine animation to the badge. |
| shinySpeed? | number                                              | 5         | Speed of the shine animation (in seconds).    |
| className?  | string                                              | -         | Additional CSS classes for the badge.         |
| children?   | React.ReactNode                                     | -         | Badge content.                                |
| ...props    | HTMLDivElement attributes                           | -         | Any other props for the badge container.      |
