---
title: "Wormhole Background - Pumki UI"
description: An immersive futuristic gateway with animated grid perspective and glowing portal core.
---

import { Tabs } from 'nextra/components'
import { CodeBlock } from '../../components/docs/code-block'
import { Preview, ResponsivePreview } from '../../components/docs/preview'
import { Button } from '../../components/ui/button'
import { WormholeBackground } from '@/components/pumki-ui/backgrounds/bg-wormhole'

# Wormhole Background 🌀

An immersive futuristic gateway with animated grid perspective and glowing portal core.

<Preview 
  code={`'use client';
 
import { WormholeBackground } from '@/components/pumki-ui/backgrounds/bg-wormhole'

export const WormholeBackgroundDemo = () => {
    return (
        <WormholeBackground className="absolute inset-0 flex items-center justify-center rounded-xl" />
    );
};`}
>
    
    <WormholeBackground className="absolute inset-0 flex items-center justify-center rounded-xl" />
    
</Preview>

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']} defaultIndex="1">
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`pnpm dlx pumki add bg-wormhole`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`npx pumki add bg-wormhole`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`yarn dlx pumki add bg-wormhole`}
    </CodeBlock>
  </Tabs.Tab>
  <Tabs.Tab>
    <CodeBlock language="bash">
      {`bun x pumki add bg-wormhole`}
    </CodeBlock>
  </Tabs.Tab>
</Tabs>

## Usage

<br/>

<CodeBlock language="typescript">
    {`import { WormholeBackground } from '@/components/pumki-ui/backgrounds/bg-wormhole';`}
</CodeBlock>

<br/>

<CodeBlock language="typescript">
    {`<WormholeBackground />`}
</CodeBlock>

## Props

| Prop              | Type                  | Default                |
|-------------------|----------------------|------------------------|
| `particleRGBColor`| `number[]`           | `[255, 255, 255]`      |
| `numberOfDiscs`   | `number`             | 50                     |
| `numberOfLines`   | `number`             | 50                     |
| `strokeColor`     | `string`             | `#737373`              |
| `className`       | `string`             | -                      |
| `children`        | `React.ReactNode`    | -                      |